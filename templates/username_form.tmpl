<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ユーザー名変更</title>
    <link rel="stylesheet" href="/static/css/main.css" />
</head>
<body>
    <div class="container">
        <h1>ユーザー名変更</h1>

        <nav class="nav">
            <a class="nav__item" href="/mypage">マイページ</a>
            <span class="nav__separator">|</span>
            <a class="nav__item" href="/">ホーム</a>
        </nav>

        <div class="card">
            <div class="card__header">
                <h2 class="card__title">新しいユーザー名を入力</h2>
            </div>
            <form method="POST" action="/mypage/username">
                <div class="card__body">
                    <div class="form__group">
                        <label class="form__label" for="username">ユーザー名</label>
                        <input type="text" id="username" name="username" class="form__input" value="{{.Username}}" required />
                    </div>
                </div>
                <div class="card__footer">
                    <button type="submit" class="btn btn--primary">変更</button>
                    <a class="btn btn--secondary" href="/mypage">キャンセル</a>
                </div>
            </form>
        </div>
    </div>
</body>
</html>
